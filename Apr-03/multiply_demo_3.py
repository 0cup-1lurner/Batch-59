# A func called multiply, consumes 2 arg : (int) x, y, returns a product (x*y)

def expression(x, y):  # Function Signature
    # Function Body
    product = (x * y) + (x + y)
   

    return f'The value is {(x * y) + (x + y)}'
